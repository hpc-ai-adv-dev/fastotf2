# Template for converter-scaling-new.ipynb's local secrets.
#
# Copy this file to local_secrets.py (which IS gitignored) and fill in your own
# values. local_secrets.py is imported automatically by the notebook when
# SYSTEM == "frontier"; it is never committed.
#
#   cp local_secrets.example.py local_secrets.py

SLURM_ACCOUNT = "your_account_here"
SLURM_MAIL_USER = "you@example.com"
