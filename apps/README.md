# Applications

This directory holds user-facing applications built on top of the FastOTF2 Chapel package.

The primary supported application will live in `apps/trace_to_csv/`.
During the migration, existing implementations may still remain under `chpl/` until they are moved.