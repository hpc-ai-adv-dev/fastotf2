# trace_to_csv

This directory is the target home for the primary FastOTF2 trace conversion application.

The existing implementation still lives under `chpl/trace_to_csv/` during the migration.
This directory exists now so subsequent steps can move code and documentation into a stable product-facing location.