# Chapel Package Source

This directory is the target Mason-friendly home for the FastOTF2 Chapel package source.

The current library implementation remains under `chpl/_chpl/` during the migration.
Once the Mason package is introduced, the reusable Chapel modules will move here.